<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Calendar.Fullscreen" parent="Base.Theme.Calendar">
        <item name="android:windowBackground">@color/background_secondary</item>
        <item name="android:windowIsTranslucent">true</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/action_bar_color</item>
        <item name="android:windowLightStatusBar">false</item><!--Use light(false) | dark(true) text/icons-->

        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/app_navbar_color</item>

        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>

    </style>

</resources>