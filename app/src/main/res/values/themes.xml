<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calendar" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <item name="colorPrimary">@color/action_bar_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item><!--Use light(false) | dark(true) text/icons-->

        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/app_navbar_color</item>

    </style>

    <style name="Theme.Calendar" parent="Base.Theme.Calendar" />


</resources>
